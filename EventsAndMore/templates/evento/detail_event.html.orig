{% extends "home_base.html" %}
{% block title %}Eventos disponibles{% endblock %}
{% block body %}
    <div class="container">
        <div class="card text-center col-12">
          <div class="card-header">
            Evento
          </div>
          <div class="card-body">
            <h5 class="card-title" style="margin-bottom: 20px">{{ evento.nombre }}</h5>
            <p class="card-text" style="margin-bottom: 15px">{{ evento.descripcion }}</p>
            <hr/>
             <div class="row">
                 <div class="col-6 col-lg-4 col-xl-3">
                    <div class="input-group">
                        <label class="input-group-text" for="inputGroupStartDate">Fecha inicio</label>
                        <input type="datetime-local" class="form-control" id="month" aria-describedby="inputGroupStartDate" value="{{ evento.fecha_inicio|date:"Y-m-d" }}T{{ evento.fecha_inicio|date:"H:i" }}" READONLY>
                    </div>
                </div>
                <div class="col-6 col-lg-4 col-xl-3">
                    <div class="input-group col-6">
                        <label class="input-group-text" for="inputGroupEndDate">Fecha fin</label>
                        <input type="datetime-local" class="form-control" id="year" aria-describedby="inputGroupEndDate" value="{{ evento.fecha_fin|date:"Y-m-d" }}T{{ evento.fecha_fin|date:"H:i" }}" READONLY>
                    </div>
                </div>
                 <div class="col-lg-4 col-xl-6"></div>
                 <div class="container">
                 <hr/>
                 </div>
                <div class="col-4 col-xl-2">
                    <div class="input-group">
                        <label class="input-group-text" for="inputGroupCapacity">Capacidad</label>
                        <input type="number" class="form-control" id="capacity" aria-describedby="inputGroupCapacity" value="{{ evento.capacidad }}" READONLY>
                    </div>
                </div>
            </div>
            <hr/>
<<<<<<< HEAD
            {% for ass in assignaciones %}
                <div>
                    <p><b>Empresa: {{ ass.cliente.nombre_empresa }}</b>Nº stand: {{ ass.stand.numero_stand }} Sector: {{ ass.cliente.sector.nombre }} </p>
                </div>
            {% endfor %}
=======
            <h2>Empresas participantes</h2>
            <div class="row row-cols-12 g-4">
            {% for assig in assigs %}
                <div class="col">
                {% if assig.estado == 'AP' %}
                    <div class="card border-success">
                {% else %}
                    <div class="card border-warning">
                {% endif %}
                      <div class="card-body">
                        <h5 class="card-title">{{ assig.cliente.nombre_empresa }}</h5>
                        <p class="card-text">N stand: {{ assig.stand.numero_stand }}</p>
                      </div>
                    </div>
                </div>
            {% endfor %}
            </div>
            <hr/>
            <h6 align="left">
                Se muestran las empresas que han hecho una solicitud tanto si esta aceptada como pendiente. De manera que dependiendo del estado de esta el borde tendra un color:
            </h6>
            <p align="left"><span class="badge bg-success"> </span> Aceptado </p>
            <p align="left"><span class="badge bg-warning"> </span> Pendiente de aprovacion </p>
            <hr/>
>>>>>>> 38eba8b1e89dbf651a700f99a8b9926816170828
            {% if user.is_cliente %}
            <a href="{% url "get_stand" evento.id %}" class="btn btn-success">Unirme</a>
            {% endif %}
          </div>
          <div class="card-footer text-muted">
            EventsAndMore® se reserva el derecho de exclusion
          </div>
        </div>
    </div>
{% endblock %}